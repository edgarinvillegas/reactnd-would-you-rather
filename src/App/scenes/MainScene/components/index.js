import React, { Fragment } from 'react';
import NavBar from "./NavBar";

export default () => {
    return (
        <Fragment>
            <NavBar />
            Main Component
        </Fragment>
    );
}